# manager_ai_agent/manager_ai_core/ 의 Neo4j 추론 파이프라인(실험·미승인)이 쓰는 의존성.
# worker_ai_agent/limo-MCP/ 는 자체 requirements.txt(ROS2/YOLO)를 따로 둔다.
neo4j

# 선택 — 없으면 axis_routing.py가 단어겹침 폴백으로 동작
sentence-transformers

# 선택 — ANTHROPIC_API_KEY가 있을 때 sequence_generator.py의 Claude 백엔드에 필요
anthropic

# frontend/ ↔ manager_ai_core/api_server.py 실험용 HTTP 게이트웨이 (실험·미승인, requests는 위 3개와 별개로
# manager_ai_agent/a2a_client/a2a_client.py 가 Worker 전달(표준 A2A, HTTP+JSON-RPC 2.0)에도 씀).
# worker_ai_agent/mcp_server/a2a_server.py(실험·미승인, D-21)도 fastapi/uvicorn을 그대로 씀 —
# rclpy/ultralytics 없이 Windows에서 단독으로 뜬다.
fastapi
uvicorn[standard]
requests
