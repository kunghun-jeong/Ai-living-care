# F-48 · 120초 완주 타임아웃도 goal 을 취소하지 않는다

> **귀속** `worker_ai_agent/action`
> **상태** ⚠ 미해소 — 소유 경계(D-17)로 생태계 담당은 고치지 않는다

**결함** 120초 완주 타임아웃도 goal 을 취소하지 않는다 (F-2 의 5초 수락 타임아웃과 별개)

**영향** 로봇은 달리는데 시퀀스는 종료, 나중에 `succeeded`로 덮인다

## 닫는 법

1. 코드를 고친다. 이 트리는 담당자 소유이므로 별도 승인이 없다.
2. **실패 경로를 한 번 실행하고 출력을 PR 에 붙인다.** 안전 결함은 「고쳤다」로 닫지 않는다.
3. **이 파일과 `worker_ai_agent/action/CLAUDE.md` 의 `상태` 줄 ID 를 함께 지운다.**
   `anchor.py` A5 가 양방향이라 한쪽만 지우면 커밋이 막힌다 — 그게 「해소했다」는 선언이다.
4. `docs/status.md` 「해소된 안전 결함」 표에 한 줄 남긴다 (실패 경로 실행 근거와 함께).
