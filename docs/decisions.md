# 결정 기록

> **한 줄이면 된다.** 결정을 내렸으면 아래 표 맨 위에 한 행. 근거가 길면 그 자리 `CLAUDE.md` 에 쓴다.
> 세션 진행 기록·작업 경위는 쓰지 않는다 — `git log` 가 그것이다.

## 정본은 여기가 아니다

| 무엇 | 정본 | 이 문서의 역할 |
|---|---|---|
| 설계 결정 `D-1`~`D-8` | spec §0.2 | 없음 |
| 구조·경계 결정 `D-9`~`D-18` | `SOT.md` §6 | 없음 |
| 결함 `F-*` · 갭 `G-*` · 선결 `U-*` | `docs/status.md` | 없음 |
| 작업 절차 | `docs/harness.md` · `docs/doc-map.md` | 없음 |
| **그 밖의 결정** | **이 문서** | **정본** |

구조나 설계를 바꾸는 결정이면 여기 한 줄을 쓰고 **정본에도 반영한다.** 반영하지 않으면
정본이 파생물보다 낡는다 — 실제로 그렇게 됐었다 (F-20).

## 결정

| 날짜 | 결정 | 정본 반영 |
|---|---|---|
| 2026-08-07 | **커밋 author 는 각자 자기 GitHub 인증 이메일로 한다** — 공용 신원(`AI-Care <noreply@skku.edu>`)으로 28개가 쌓여 아무에게도 귀속되지 않았다. 과거 이력은 재작성하지 않는다(clone 파손 위험). **AI 공동저자 트레일러는 넣지 않는다** | `CONTRIBUTING.md` 「커밋 신원」 |
| 2026-08-07 | **브랜치를 2단으로 — `master` 가 협업자 통합 브랜치, `main` 은 소유자가 PR 로 승격하는 최종본** | `CONTRIBUTING.md` 「브랜치」 |
| 2026-08-07 | **`main` 직접 푸시를 훅으로 막는다** — 규약 문장만으로는 안 지켜진다. `.githooks/pre-push` 가 **목적지**로 판정하므로 `HEAD:main` 우회도 막힌다. `master` 푸시와 브랜치 삭제, `SKIP_MAIN_PUSH=1` 은 통과 | `.githooks/pre-push` |
| 2026-08-07 | **CI 를 `master` 푸시에서도 돌린다** — `master` 는 PR 을 안 거치므로 여기서 안 돌면 검사 없이 흘러간다 | `.github/workflows/check.yml` |
| 2026-08-07 | **한 방향만 보는 검사는 지우는 쪽을 못 막는다** — A5 를 양방향으로, A1 의 본문 부분문자열 폴백을 제거하고 표 행 유령 검사를 추가, A7(문서 경로 실재·유일 축약) 신설 | `anchor.py` A1·A5·A7 |
| 2026-08-07 | **F-28 을 「해소」에서 「미해소」로 되돌린다** — 기록된 조치가 구현된 적이 없었다. 장치보다 기록을 사실로 만드는 것이 먼저다 | `docs/status.md` |
| 2026-08-07 | **`.gitignore` 도 앵커 대상이다** — 슬래시가 든 패턴은 루트 고정이라 디렉터리를 옮기면 조용히 죽는다. 재구조화 때 실제로 3줄이 죽어 텍스처 수백 장이 들어올 뻔했다 | `anchor.py` A6 |
| 2026-08-07 | **안전 결함은 소유 경계를 우회하는 통로를 갖는다** — `status.md` 「안전 — 담당자 통지」 표 + 컴포넌트 `상태` 줄의 짝. `anchor.py` A5 가 강제 | `SOT.md` §6 `D-18` |
| 2026-08-07 | **`CODEOWNERS` 를 두지 않는다** — 담당자가 미배정이고 한 사람이 한 프로젝트만 맡지 않는다. 경계는 PR 설명·리뷰로 지킨다 | `CONTRIBUTING.md` |
| 2026-08-07 | **`@` 는 루트에서만 쓴다** — `@` 는 재귀 import(최대 5홉)라 하위 문서에 하나 붙으면 자동 로딩이 눈덩이가 된다. **문서에 실측 줄수를 적지 않는다**(낡는다) — `make status` 가 잰다 | `anchor.py` A4 |
| 2026-08-06 | **상태 수확은 하되 파일에 쓰지 않는다** (`make status`) — 4인 병렬 실측: 평면 수확 충돌 3/4 · 계층 수확 1/4 · 미수확 0/4 | `anchor.py --status` |
| 2026-08-06 | **컴포넌트 헤더는 `역할 / 상태 / 읽을 절 / 정본` 4줄, 위계 라벨은 쓰지 않는다** — 경로가 곧 위계다. 라벨은 신규 컴포넌트마다 밀린다 | 48개 `CLAUDE.md` |
| 2026-08-06 | **문서는 3계층** — `@` 자동 로딩 / gateway(`doc-map`) / 무표 lazy. spec 962줄은 헤더가 지목한 절만 | `CLAUDE.md` · `docs/doc-map.md` |
| 2026-08-06 | **하네스를 앵커 규칙 하나로 축소** — 감사 기구·사전 점검·검증 관문·리스크 등급 R0~R4 폐기. 남긴 규칙: 파일·디렉터리가 생기거나 바뀌면 그 자리 `CLAUDE.md` 에 한 줄, 결정은 여기 한 줄 | `docs/harness.md` |
| 2026-08-06 | **`CLAUDE.md` 생성기(`sot_migrate.py` · `sot_preserve.py`) 폐기**, 수기 유지로 전환 — 42개 문서의 실질 정본이 파이썬 문자열 딕셔너리였다 (문서 결함의 단일 최대 원인) | `sot_audit.py` `ROOT_PY_ALLOW` |
| 2026-08-06 | **`sot_audit.py` 는 구조를 바꿀 때만 쓰는 도구**로 강등. 상시 관문은 `anchor.py` 하나 | `Makefile` |

## 감사 폐기로 포기한 보증

2026-08-06 에 `doc_audit.py`(장치 11종)를 폐기했다. **얻은 것은 비용이고 잃은 것은 아래다.**
어느 것도 문서에 남아 있지 않았다 — 나중에 F-20 계열(정본이 파생물보다 낡음)이 재발하면
원인을 추적할 수 없다. 되살릴지는 그때 판단하되, **무엇을 포기했는지는 알고 있어야 한다.**

| 옛 장치 | 보증하던 것 | 지금 |
|---|---|---|
| `DA-1` | `@mcp.tool()` 집합 = `api-spec.md` · `mcp_server/CLAUDE.md` | **유지** → `anchor.py` A2 |
| `DA-2` | 부모 `CLAUDE.md` ⊇ 실제 자식 디렉터리 | **유지** → A1 (2026-08-07 양방향으로 강화) |
| `DA-3` | `SOT.md` §2 트리 = `sot_audit.py` 검사 대상 = 실제 디렉터리 **3자 집합 일치** | **포기** — D-16 이 규범으로만 남았다. F-20 이 정확히 이 구멍이었다 |
| `DA-4` | 「N종」 리터럴 = 정본 개수 (tool 6종 · dispatch-mode 4종 …) | **포기** — F-26 유형(문서 3건이 「5종」)이 재발해도 안 잡힌다 |
| `DA-5` | 참조된 ID(`F/G/U/S/D/N/SP/AR/P/V/IF-*`)가 정의처에 실재 | **포기** — spec 절 번호만 A3 가 본다. 없는 `F-99`·`G-9` 를 인용해도 통과 |
| `DA-6` | 보존 대상(D-14) blob = `27b0f30`. **작업 트리를 본다 — 커밋 전에 걸린다** | **포기** — D-17 경계 위반을 기계가 못 잡는다. 2026-08-06 에 8개 파일을 넘어간 그 사고다 |
| `DA-7` | 문서가 적은 경로 실재 | **복원** (2026-08-07) → A7. 축약은 유일 해석만 허용 |
| `DA-8` | 폐기 생성기(`sot_migrate.py`·`sot_preserve.py`)가 `ROOT_PY_ALLOW` 로 부활하지 않음 | **포기** — `sot_audit.py` 목록에서 뺐을 뿐 봉인 검사는 없다 |
| `DA-9` | `CLAUDE.md` 헤더 규약 · `@` 참조 실재 · 루트 50줄 | **부분 복원** → A4 가 루트 50줄 + `@` 재귀 총합. **4줄 헤더 규약 검사는 포기** |
| `DA-10` | `architecture.md` 컴포넌트 표 = 실제 디렉터리 | **포기** — 그림만 옛 구조로 남는다. F-62(IF-4·IF-5 불일치)가 그 상태다 |
| `DA-11` | **장치·규칙이 실제로 실행됐는가** (감사기 자기 감사) | **포기** = `F-28`. 「해소」로 오표기돼 있던 것을 2026-08-07 에 되돌렸다 |

> 포기 6건(`DA-3`·`4`·`5`·`8`·`10`·`11`) 중 **`DA-3`·`DA-11` 만 지금도 실증된 구멍**이다
> (F-20 재발 경로 · F-28). 나머지 넷은 아직 사고가 없다. 되살리는 순서는 그 순이다.
